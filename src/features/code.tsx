import React from 'react';
import { ReactNode } from 'react';

export interface CodeProps {
  
}

export const Code = ({
  
}: CodeProps) => {
  return (
    <pre>
      const birthday = new Date(”1990-04-18”); const location =
      “Florianópolis, Brazil”; const pronouns = [“he”, “him”]; const
      degree = “Internet Systems Technologist (FEEVALE, 2011)”; const
      secondDegree = “Bachelor in Sociology”; const values = [“ethic”,
      “love”, “freedom”];
    </pre>
  );
};